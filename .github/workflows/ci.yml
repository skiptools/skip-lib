name: skip-lib
on:
  push:
    branches: [ main ]
    tags: "[0-9]+.[0-9]+.[0-9]+"
  schedule:
    - cron:  '0 19 * * *'
  workflow_dispatch:
  pull_request:

permissions:
  contents: write

jobs:
  call-workflow:
    #uses: skiptools/actions/.github/workflows/skip-framework.yml@v1
    uses: skiptools/actions/.github/workflows/skip-framework.yml@linux
    with:
      runs-on: "['macos-13', 'ubuntu-latest']"

